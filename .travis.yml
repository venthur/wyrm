language: "python"

python:
  - "2.7"
  - "3.3"

install: 
  - sudo apt-get install python-numpy, python3-numpy, python-scipy, python3-scipy, python-matplotlib, python3-matplotlib, python-sklearn
    ## numpy build-dependencies
    #  - "sudo apt-get install python-all-dev python3-all-dev python-support gfortran libblas-dev liblapack-dev python-nose"
    ## numpy dependencdies
    #  - "sudo apt-get install libblas3gf liblapack3gf libatlas3gf-base"
    ## scipy build-dependencies
    #  - "sudo apt-get install python-all-dev python3-all-dev cython gfortran libblas-dev liblapack-dev libatlas-base-dev"
    ## scipy dependencies
    #  - "sudo apt-get install libblas3gf liblapack3gf libatlas3gf-base"
  - "pip install -r requirements.txt --use-mirrors"

script: "./testall.sh"

